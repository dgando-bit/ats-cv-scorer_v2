services:
  backend_ats:
    volumes:
      - ./backend/app:/app/app
      - ./backend/tests:/app/tests
      - ./backend/scripts:/app/scripts
      - ./backend/data:/app/data

    command: >
      uv run uvicorn app.main:app
      --host 0.0.0.0
      --port 8000
      --reload